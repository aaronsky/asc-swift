name: SwiftPM

on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/swiftpm.yml'
      - 'Examples/**/*.swift'
      - 'Package*'
      - 'Sources/**/*.swift'
      - 'Tests/**/*.swift'
  pull_request:
    paths:
      - '.github/workflows/swiftpm.yml'
      - 'Examples/**/*.swift'
      - 'Package*'
      - 'Sources/**/*.swift'
      - 'Tests/**/*.swift'
  workflow_dispatch: ~

concurrency:
  group: swiftpm-${{ github.ref }}
  cancel-in-progress: true

jobs:
  macOS:
    runs-on: macos-14
    strategy:
      matrix:
        xcode: ['15.4', '16.1_beta']
    env:
      DEVELOPER_DIR: /Applications/Xcode_${{ matrix.xcode }}.app/Contents/Developer
    steps:
      - uses: actions/checkout@v4
      - run: swift -version
      - run: swift test --parallel
  Linux:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        tag: ['5.9-focal', '5.10-focal', 'nightly-6.0-focal']
    container:
      image: swift:${{ matrix.tag }}
    steps:
      - uses: actions/checkout@v4
      - run: swift test --parallel
